<script setup lang="ts">
import { inBrowser } from 'vitepress'

if (inBrowser) {
  window.location.replace('/scenario/overview')
}
</script>

# 路径已废弃

企业组件库文档站公开模块已调整为 `组件` 和 `场景模板`。

- 查看组件文档：[组件总览](/component/overview)
- 查看组合模板：[场景模板](/scenario/overview)
