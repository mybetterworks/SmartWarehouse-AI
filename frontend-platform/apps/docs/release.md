<script setup lang="ts">
import { inBrowser } from 'vitepress'

if (inBrowser) {
  window.location.replace('/component/overview')
}
</script>

# 路径已废弃

版本信息不再作为组件文档站公开模块展示。平台包版本与发布记录以制品库、根目录 README 和 `docs/plan` 实现记录为准。

请进入 [组件总览](/component/overview) 或 [场景模板](/scenario/overview)。
